
ELASTIC SEARCH WITHOUT VPC

ELK_IAM_ROLE.yaml will create a role in IAM for elastic search so that it can access VPC.
Upload this template first in CFT.

ELK_WITHOUT_VPC.yaml will create a ELASTICSEARCH in AWS with VPC.
Upload this template now in CFT.

Click on kibana link while going to ES service in AWS and you can access the kibana link.